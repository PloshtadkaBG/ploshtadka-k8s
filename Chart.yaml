apiVersion: v2
name: ploshtadka
description: PloshtadkaBG sports venue booking platform
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  # Cache layer — standard app, no CRDs, safe as a dependency
  - name: redis
    version: ">=20.0.0-0"
    repository: https://charts.bitnami.com/bitnami

# Traefik and CloudNativePG are NOT listed here because they install CRDs
# that must exist before this chart's templates are rendered.
# Install them once as prerequisites (see scripts/prerequisites.sh).
