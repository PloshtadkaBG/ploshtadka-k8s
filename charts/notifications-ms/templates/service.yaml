apiVersion: v1
kind: Service
metadata:
  name: notifications-ms
  labels:
    {{- include "notifications-ms.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "notifications-ms.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
